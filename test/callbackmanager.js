var assert = require( "assert" )
var CallbackManager = require( '../lib/callbackmanager.js' );

describe( 'Series Manager', function () {
    var callbackmanager = new CallbackManager( 'series' );
    var output = [];
    
    before( 'run series' 
    
    );

    it( 'should ', function (done) { 
        
    } );
} );